import React from "react";
import styled from "styled-components";
import Layout from "../Layout";

const Subscribe = () => {
  return (
    <Layout active="subscribe">
      <div>Finder</div>
    </Layout>
  );
};

export default Subscribe;
